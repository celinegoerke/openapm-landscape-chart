apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmchartbachelor.fullname" . }}-test-connection"
  labels:
    {{- include "helmchartbachelor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmchartbachelor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
